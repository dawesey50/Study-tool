{
  "name": "processor",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first study system for a biomedical sciences degree",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "start": "npm run start --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "test:e2e": "node e2e/smoke.mjs",
    "app": "node scripts/launch.mjs",
    "doctor": "node scripts/doctor.mjs",
    "seed": "node scripts/seed.mjs",
    "check": "npm run typecheck && npm test",
    "db:generate": "npm run db:generate --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.11"
  }
}
