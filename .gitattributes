# Batch files must keep CRLF or cmd.exe can misread them.
*.bat text eol=crlf
*.command text eol=lf
*.sh text eol=lf

# Fixtures are binary; never let git rewrite their line endings.
*.pdf binary
*.png binary
